#Week - 4
#Ordered Weighted Averaging Example  - Slide 4-5
diverseScores <- c(9,8.8,9.6,4.3,7.6,8)
diverseWeights<- c(0,0.25,0.25,0.25,0)

#Note that the OWA function sorts the score , which is why the weights in diversewight looks 
#different than in the lecture slides

OWA(diverseScores,diverseWeights)


